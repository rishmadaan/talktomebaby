{
  "name": "talktomebaby-suite",
  "private": true,
  "version": "0.0.0",
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run build -w @talktomebaby/engine && npm run build --workspaces --if-present",
    "pretest": "npm run build -w @talktomebaby/engine",
    "test": "npm run test --workspaces --if-present",
    "compile": "npm run build -w @talktomebaby/engine && npm run compile -w talktomebaby",
    "prewatch": "npm run build -w @talktomebaby/engine",
    "watch": "npm run watch -w @talktomebaby/engine & npm run watch -w talktomebaby",
    "package": "npm run package -w talktomebaby"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^5.3.0",
    "vitest": "^3.0.0"
  }
}
